rspb5:

@GetMapping("/employee")
	public List<Employee> getAllCourses(){
	 return employeeService.getAllEmployees();
	}
	
	@GetMapping("/employee")
	public  ResponseEntity getAllCourses(){     giving error  OR new ArrayList(<>) need to add for this case
	 return employeeService.getAllEmployees();
	}
	
rspb7:

import 'bootstrap/dist/css/bootstrap.css'; at index.js after npm i bootstrap 

To check in the app.css -gives errors. so, change to sass or less to import add.scss will give same result or error